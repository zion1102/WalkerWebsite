# Walker Photography Portfolio

Starter React + Vite portfolio website with:

- Earth-tone, minimal visual style
- Pages: Landing, Prints, Bookings, Admin Portal
- Admin-managed image list (stored in browser localStorage)
- Per-image download toggle (default: disabled)

## Run locally

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
npm run preview
```
